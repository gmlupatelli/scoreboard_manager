# Supabase Configuration
# Copy this file to .env.local and fill in your values
# Never commit .env files with real secrets to the repository

# ============================================================
# Runtime Environment Variables (Required for Next.js app)
# ============================================================

# Supabase Project URL
# Get this from: https://app.supabase.com/project/[project-ref]/settings/api
# Format: https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Supabase Publishable Key (Public API Key)
# Get this from: https://app.supabase.com/project/[project-ref]/settings/api
# Under "Project API keys" section → Use the "anon" key (legacy) or "Publishable" key (modern)
# This key is safe to expose in client code (used for auth and RLS)
# NOTE: Variable name updated to NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY for consistency
#       You can use either the legacy "anon" key or modern "Publishable" key here
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Supabase Secret Key (Admin API Key - KEEP SECRET!)
# Get this from: https://app.supabase.com/project/[project-ref]/settings/api
# Under "Project API keys" section → Use the "service_role" key (legacy) or "Secret" key (modern)
# This key has full database access - never expose in client code
# Only use in server-side API routes or serverless functions
# NOTE: Variable name updated to SUPABASE_SECRET_KEY for consistency
#       You can use either the legacy "service_role" key or modern "Secret" key here
SUPABASE_SECRET_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ============================================================
# Supabase CLI Environment Variables (Required for migrations)
# ============================================================

# Supabase Personal Access Token
# Get this from: https://app.supabase.com (Profile → Account Settings → Access Tokens)
# Click "Generate New Token", name it "Local Development CLI"
# Format: sbp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
SUPABASE_ACCESS_TOKEN=sbp_your_personal_access_token_here

# Supabase Database Password
# Get this from: https://app.supabase.com/project/[project-ref]/settings/database
# Under "Connection string", reveal and copy the password
SUPABASE_DB_PASSWORD=your_database_password_here

# Supabase Project Reference ID (Development)
# Get this from: https://app.supabase.com/project/[project-ref]/settings/general
# The "Reference ID" field (16 characters, alphanumeric)
# Example: abcdefghijklmnop
SUPABASE_PROJECT_REF_DEV=your_dev_project_ref_here

# Supabase Project Reference ID
# Get this from: https://app.supabase.com/project/[project-ref]/settings/general
# The "Reference ID" field (16 characters, alphanumeric)
# Use context-specific value:
#   - Dev: xxxxxxxxxxxxxxxxxxx
#   - Prod: xxxxxxxxxxxxxxxxxxx
# For Netlify: Set different values per deploy context
SUPABASE_PROJECT_REF=your_project_ref_here

# Google OAuth (optional)
# Client-side (public): Google OAuth Client ID (Web client)
# Example: 224411728988-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx.apps.googleusercontent.com
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Server-side (secret): Google client secret used for configuring Supabase locally
SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_SECRET=your-google-client-secret

# Enable Google One‑Tap (optional): set to "true" to enable One‑Tap sign-in
# Default: false
NEXT_PUBLIC_ENABLE_GOOGLE_ONE_TAP=false
